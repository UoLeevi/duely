import React from 'react';
import LoadingSpinner from '../LoadingSpinner';

export default {
  title: 'LoadingSpinner',
  component: LoadingSpinner,
};

export const FixedSize = () => (
  <LoadingSpinner size="5em" />
);
