schema: "https://api.duely.app/graphql"
config:
  dedupeFragments: true
documents: "./src/**/*.graphql"
generates:
  ./src/typed-document-nodes.ts:
    plugins:
      - typescript
      - typescript-operations
      - typed-document-node
