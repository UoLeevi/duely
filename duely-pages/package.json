{
  "name": "duely-pages",
  "type": "module",
  "version": "0.0.1",
  "description": "Duely website based on wev components and markdoc.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "tsc --watch",
    "start": "web-dev-server"
  },
  "dependencies": {
    "@markdoc/markdoc": "^0.1.2",
    "lit": "^2.2.5",
    "typescript": "^4.7.2"
  },
  "devDependencies": {
    "@web/dev-server": "^0.1.31",
    "@web/dev-server-esbuild": "^0.3.0"
  }
}
